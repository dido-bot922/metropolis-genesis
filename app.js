const canvas=document.getElementById('cityCanvas'),ctx=canvas.getContext('2d');const COLS=34,ROWS=24;let cell=30,ox=0,oy=0,tool='inspect',paused=false,speed=1,heat=false,drag=false,lastCell=null,toastTimer;const state={money:24800,pop:24,jobs:18,happiness:78,traffic:12,pollution:8,day:1,hour:8,weather:'Céu limpo'};const grid=Array.from({length:ROWS},()=>Array.from({length:COLS},()=>({type:'empty',level:0})));[[16,12],[17,12],[18,12],[19,12],[20,12],[21,12],[22,12],[23,12],[24,12],[16,13],[16,14],[16,15],[16,16],[20,13],[20,14],[20,15],[20,16]].forEach(([x,y])=>grid[y][x].type='road');[[14,10],[15,10],[14,11],[15,11],[14,16],[15,16],[17,10],[18,10],[19,10]].forEach(([x,y])=>grid[y][x]={type:'residential',level:1});[[22,10],[23,10],[22,11]].forEach(([x,y])=>grid[y][x]={type:'commercial',level:1});[[22,15],[23,15]].forEach(([x,y])=>grid[y][x]={type:'industrial',level:1});
const colors={residential:'#397da5',commercial:'#b17b3b',industrial:'#975394',road:'#36444d',school:'#4b83bc',clinic:'#b64d7d',park:'#3f9368',police:'#7258aa'};function resize(){const r=canvas.getBoundingClientRect(),d=devicePixelRatio||1;canvas.width=r.width*d;canvas.height=r.height*d;ctx.setTransform(d,0,0,d,0,0);cell=Math.min(34,(r.height-50)/ROWS);ox=(r.width-COLS*cell)/2;oy=(r.height-25-ROWS*cell)/2}window.addEventListener('resize',resize);resize();
function draw(){const w=canvas.clientWidth,h=canvas.clientHeight;ctx.clearRect(0,0,w,h);let night=Math.max(0,(state.hour-18)/7)+Math.max(0,(7-state.hour)/7);ctx.fillStyle=night>.4?'#102129':'#1c3531';ctx.fillRect(0,0,w,h);for(let y=0;y<ROWS;y++)for(let x=0;x<COLS;x++){let c=grid[y][x],px=ox+x*cell,py=oy+y*cell;ctx.fillStyle=heat&&c.type==='road'?`rgba(230,${Math.max(35,170-state.traffic*3)},60,.65)`:(c.type==='empty'?'#20413d':'#1b3937');ctx.fillRect(px+1,py+1,cell-2,cell-2);if(c.type==='road'){ctx.fillStyle='#3c4b51';ctx.fillRect(px,py,cell,cell);ctx.strokeStyle='#b9ad72';ctx.setLineDash([5,6]);ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(px+cell/2,py+2);ctx.lineTo(px+cell/2,py+cell-2);ctx.stroke();ctx.setLineDash([])}else if(c.type!=='empty'){drawBuilding(px,py,c)}else{ctx.fillStyle='#1b4b42';ctx.globalAlpha=.45;ctx.fillRect(px+4,py+4,cell-8,cell-8);ctx.globalAlpha=1}}
// roads highlights
ctx.strokeStyle='#83ad9a22';ctx.lineWidth=2;for(let y=0;y<=ROWS;y++){ctx.beginPath();ctx.moveTo(ox,oy+y*cell);ctx.lineTo(ox+COLS*cell,oy+y*cell);ctx.stroke()}for(let x=0;x<=COLS;x++){ctx.beginPath();ctx.moveTo(ox+x*cell,oy);ctx.lineTo(ox+x*cell,oy+ROWS*cell);ctx.stroke()}drawVehicles();if(night>.2){ctx.fillStyle=`rgba(5,12,29,${Math.min(.55,night*.5)})`;ctx.fillRect(0,0,w,h)}}
function drawBuilding(px,py,c){let col=colors[c.type],pad=cell*.13,h=cell*(.42+c.level*.12);ctx.fillStyle=col+'55';ctx.fillRect(px+pad,py+cell-h-pad,cell-pad*2,h);ctx.fillStyle=col;ctx.fillRect(px+pad+2,py+cell-h-pad+2,cell-pad*2-4,h-2);ctx.fillStyle='#d8e6df88';for(let i=0;i<3;i++)for(let j=0;j<Math.min(3,c.level+1);j++)ctx.fillRect(px+pad+5+i*6,py+cell-h+7+j*7,3,3);if(c.type==='park'){ctx.fillStyle='#73d18b';ctx.beginPath();ctx.arc(px+cell/2,py+cell*.48,cell*.22,0,7);ctx.fill()}if(['school','clinic','police'].includes(c.type)){ctx.fillStyle='#f4e5b4';ctx.font=`${cell*.32}px Arial`;ctx.textAlign='center';ctx.fillText(c.type==='clinic'?'✚':c.type==='school'?'✧':'◆',px+cell/2,py+cell*.62)}}
function drawVehicles(){let t=Date.now()/700;for(let i=0;i<Math.min(18,state.traffic+4);i++){let x=(i*7.3+t*(.15+i%3*.04))%COLS,y=(i*5+2)%ROWS;if(grid[y]&&grid[y][Math.floor(x)]?.type==='road'){ctx.fillStyle=i%3?'#e7a866':'#77c6d6';ctx.beginPath();ctx.arc(ox+x*cell+cell/2,oy+y*cell+cell/2,3,0,7);ctx.fill()}}}
function pos(e){const r=canvas.getBoundingClientRect();return{x:Math.floor((e.clientX-r.left-ox)/cell),y:Math.floor((e.clientY-r.top-oy)/cell)}}function act(x,y){if(x<0||y<0||x>=COLS||y>=ROWS)return;let c=grid[y][x];if(tool==='inspect'){showInspect(c,x,y);return}if(tool==='bulldoze'){if(c.type!=='empty'){grid[y][x]={type:'empty',level:0};state.money+=Math.floor(80);notify('Terreno liberado · +$80')}return}let cost=tool==='road'?120:['school','clinic','police','park'].includes(tool)?({school:1200,clinic:1600,police:1000,park:500}[tool]):25;if(state.money<cost){notify('Orçamento insuficiente');return}if(c.type!=='empty'&&tool!=='road'){notify('Esse terreno já está ocupado');return}grid[y][x]={type:tool,level:1};state.money-=cost;notify(tool==='road'?'Estrada construída':tool==='park'?'Parque inaugurado':'Construção concluída');update()}
canvas.addEventListener('pointerdown',e=>{drag=true;canvas.setPointerCapture(e.pointerId);let p=pos(e);lastCell=p;act(p.x,p.y)});canvas.addEventListener('pointermove',e=>{if(!drag)return;let p=pos(e);if(!lastCell||p.x!==lastCell.x||p.y!==lastCell.y){lastCell=p;act(p.x,p.y)}});canvas.addEventListener('pointerup',()=>{drag=false;lastCell=null});canvas.addEventListener('contextmenu',e=>{e.preventDefault();setTool('inspect')});
function setTool(t){tool=t;document.querySelectorAll('.tool,.service').forEach(b=>b.classList.toggle('active',b.dataset.tool===t));document.getElementById('toolLabel').textContent=t==='inspect'?'Selecione uma ferramenta':({road:'Construir estrada',residential:'Pintar zona residencial',commercial:'Pintar zona comercial',industrial:'Pintar zona industrial',bulldoze:'Demolir',school:'Posicionar escola',clinic:'Posicionar clínica',park:'Posicionar parque',police:'Posicionar segurança'}[t]||t)}document.querySelectorAll('[data-tool]').forEach(b=>b.onclick=()=>setTool(b.dataset.tool));
function showInspect(c,x,y){let card=document.getElementById('inspectCard');card.classList.remove('hidden');document.getElementById('inspectTitle').textContent=c.type==='empty'?'Terreno vazio':c.type==='road'?'Estrada local':c.type.replace(/^./,a=>a.toUpperCase());document.getElementById('inspectText').textContent=c.type==='empty'?`Setor ${x+1}-${y+1}. Selecione uma ferramenta para transformar este terreno.`:`Nível ${c.level||1} · Clique em Demolir para remover esta construção.`}document.getElementById('closeInspect').onclick=()=>document.getElementById('inspectCard').classList.add('hidden');function notify(s){let t=document.getElementById('toast');t.textContent=s;t.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.classList.remove('show'),1700)}
function update(){let counts={residential:0,commercial:0,industrial:0,road:0};grid.flat().forEach(c=>counts[c.type]=(counts[c.type]||0)+1);state.pop=Math.max(0,20+counts.residential*4+Math.floor((state.day-1)*2));state.jobs=12+counts.commercial*5+counts.industrial*7;state.traffic=Math.min(98,Math.max(5,Math.floor(counts.road*1.7+state.pop/5)));state.happiness=Math.max(35,Math.min(99,80-Math.floor(state.pollution/3)-(state.traffic>60?10:0)));document.getElementById('money').textContent='$ '+Math.floor(state.money).toLocaleString('pt-BR');document.getElementById('population').textContent=state.pop;document.getElementById('jobs').textContent=`${Math.min(state.jobs,state.pop)} / ${state.pop}`;document.getElementById('happiness').textContent=state.happiness+'%';document.getElementById('traffic').textContent=state.traffic+'%';document.getElementById('trafficState').textContent=state.traffic<35?'fluido':state.traffic<65?'moderado':'congestionado';document.getElementById('trafficState').className=state.traffic<65?'positive':'';document.getElementById('income').textContent='$ '+Math.floor(200+state.pop*5)+' /dia';let ds=[Math.min(95,62+state.pop%20),Math.min(90,30+counts.commercial*8),Math.min(90,25+counts.industrial*7)];['res','com','ind'].forEach((v,i)=>{document.getElementById(v+'Demand').textContent=ds[i]+'%';document.getElementById(v+'Bar').style.width=ds[i]+'%'});document.getElementById('pollution').textContent='● '+state.pollution+'%';document.getElementById('dateLabel').textContent=`Dia ${state.day} · ${String(Math.floor(state.hour)).padStart(2,'0')}:00`}
function tick(){if(!paused){state.hour+=.025*speed;if(state.hour>=24){state.hour=0;state.day++;state.money+=200+state.pop*5;state.pollution=Math.min(90,state.pollution+(grid.flat().filter(c=>c.type==='industrial').length?1:0));update()}}draw();requestAnimationFrame(tick)}
document.getElementById('pauseBtn').onclick=()=>{paused=!paused;document.getElementById('pauseBtn').textContent=paused?'▶':'Ⅱ'};document.querySelectorAll('.speed').forEach(b=>b.onclick=()=>{speed=+b.dataset.speed;document.querySelectorAll('.speed').forEach(x=>x.classList.remove('active'));b.classList.add('active')});document.getElementById('heatBtn').onclick=()=>{heat=!heat;document.getElementById('heatBtn').textContent=heat?'⌁ Ocultar mapa de tráfego':'⌁ Mostrar mapa de tráfego'};document.getElementById('saveBtn').onclick=()=>{localStorage.metropolis=JSON.stringify({state,grid});notify('Cidade salva neste navegador')};document.getElementById('loadBtn').onclick=()=>{try{let s=JSON.parse(localStorage.metropolis);Object.assign(state,s.state);s.grid.forEach((r,y)=>r.forEach((c,x)=>grid[y][x]=c));update();notify('Cidade carregada')}catch(e){notify('Nenhum salvamento encontrado')}};document.getElementById('helpBtn').onclick=()=>notify('Atalhos: V inspeção · R estrada · 1/2/3 zonas · B demolir · Espaço pausa');window.addEventListener('keydown',e=>{if(e.code==='Space'){e.preventDefault();document.getElementById('pauseBtn').click()}let k={v:'inspect',r:'road','1':'residential','2':'commercial','3':'industrial',b:'bulldoze'}[e.key.toLowerCase()];if(k)setTool(k)});update();tick();
